# Minibot `build/`

Minibot buillding directory

To build/compile execute the following commands:
```bash
cmake ..
make
```

- Libraries are generated in `lib/`
- Testing binaries are generated in `bin/`
